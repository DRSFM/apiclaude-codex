# 多账户升级修复与复核

日期：2026-09-17。基线：`34013e6`；本记录随多账户功能提交。
依据：用户提供的同日独立验收报告及“做完然后 review 完记得 git”指令。

## 结论

多账户 CLI、OAuth 导入、独立认证、账号菜单与共享资源已实现并部署。
独立验收发现的嵌套功能配置阻断已修复，重复导入的提示和 README 状态已修正。
源码 review 未发现本次提交范围内仍需阻断交付的问题；Desktop 完整验收尚未结束，
不能把单元测试、账号身份读回或关闭重开窗口等同于 Desktop 进程重启验收。

## 本轮修复

- 共享 `features` 支持嵌套表、点号键和根内联表，保留值的 TOML 文本及键的层级。
  仅排除 `features.secret_auth_storage` 子树，避免共享源改变具名账号认证存储；
  带点号的单个引号键不与层级键混淆。无新增依赖，保持 Python 3.10 语法兼容。
- 重复 OAuth 导入仍优先保留官方已刷新的凭据，不自动重放旧导出文件。
  命令行和交互菜单统一说明：仅复用 Profile，未检查或恢复登录。
  退出后可用 `account login NAME`，或指定现有 Profile 和当前导出文件进行 `--update`。
- README 已记录用户进入 Desktop 的反馈，删除“仍待首次填写”的过时结论。

## 验证与 review

- 先在旧实现复现新增回归的失败，再验证修复：嵌套写法、值保留、空父表、
  引号键、认证隔离与幂等合并均通过。
- 完整 `python -m pytest tests/ -q`：335 passed、15 skipped、206 subtests passed。
- 单独启用官方 CLI 0.154.0 原生测试：2 passed、3 subtests passed。
  三种共享嵌套配置均可由官方 app-server 加载并执行 `account/read`；
  临时假凭据测试覆盖加密存储、双账户并行、新进程读取和独立退出，清理临时凭据。
- Review 检查共享配置过滤、导入复用/回滚、环境清理、CLI/Desktop 路由、
  归档保护、会话交接和旧管理入口保护。Python 3.10 语法检查、编译和 diff 检查通过。
  Python 3.10 本身未实机运行；旧 Tauri 仍未构建，不作为支持的账户管理入口。
- 三个修复模块已备份部署至 `C:\tools`，根目录 20 个运行模块 SHA-256 全部一致。
  备份：`C:\tools\backups\apicodex-account-review-20260917-155726`。
- 从仓库外调用全局入口，两账号均解析到官方 CLI 0.154.0；同步前后认证密文哈希相等，
  官方读取身份均与所选 Profile 一致。Skills/rules/prompts 联接均正确。
- 本轮没有重复执行真实模型请求或刷新令牌实验；之前的 A/B 回复、刷新和手动历史
  交接结果仍是历史验证证据，不冒充本轮重测。

## 尚未验收

- 两个具名 Desktop 当前均有运行进程。完整退出再启动可能中断正在执行的任务，
  已询问是否可以重启；答复前保留现有进程。
- 两个账号的 `plugins/cache` 当前均未联接到默认源，待释放占用后再次同步验证。
- Desktop 完整进程重启后的 UI 身份保持、连接器各自授权仍待验。
- 自动跨 Profile 派发任务和结果回传不在本轮范围；现有能力是显式复制可见历史后接续。

## 提交边界

本次提交包含此前尚未入库的多账户功能及本轮修复、测试和文档。
OAuth 原始文件、真实账号配置、凭据和本机验收产物不纳入 Git。
用户授权本地 Git 提交；远端推送未执行。
