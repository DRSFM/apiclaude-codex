# 会话迁移面板设计验收

## 验收基线

- 参考图：`C:\Users\神风默\AppData\Local\Temp\codex-clipboard-cc5cfd7e-ac1d-4291-afff-f0c62916daa3.png`
- 实现图：`design-qa-implementation.png`
- 对照图：`design-qa-comparison.png`
- 聚焦对照图：`design-qa-focused.png`
- 视口：1514 × 1017 CSS px
- 像素与密度：参考图 1491 × 1055 @ 96 DPI；实现图 1514 × 1017 @ 96 DPI，
  均按 1× 密度判断。参考图是静态 Windows 窗口截图，无法重新设置视口，因此
  全局对照保留各自原始像素并顶端对齐；聚焦对照按相同内容宽度缩放参考迁移区，
  不用两者的窗口边缘差异判断间距。
- 页面状态：账号态为来源，已选择一个空闲会话，`muyuan` 为目标，迁移按钮已启用
- 页面数据：来自本机真实 ApiCodex Profile、Codex app-server 会话列表和 EFS 共享池

## 全局对照

- 保留了参考图的浅色桌面管理器、左侧导航、顶栏搜索、暖金强调色、四段式迁移流程和底部记录区。
- 按当前项目能力收敛为单会话安全复制，不展示尚未实现的多账号登录、厂商延迟、批量任务、暂停/回滚和冲突策略。
- 来源和目标均支持账号态及所有已添加的 ApiCodex Profile，目标自动排除当前来源。
- 迁移策略以只读方式呈现，明确可见上下文保留、私密运行态清除、独立线程和目标配置物化。

## 必查视觉面

- 字体与层级：沿用项目现有系统字体；标题、卡片标题、说明和元数据使用与参考图
  相近的四级层级，长标题和路径在卡片内截断，不再破坏网格。
- 间距与布局：四列步骤、卡片内边距、细分隔线和底部记录区与参考图同构；
  实现去掉了无能力支撑的账号/API 总览，迁移流程因此上移并获得更完整的首屏空间。
- 颜色与令牌：暖白画布、浅米卡片、低对比边框、暖金选择/主操作、绿色可用状态
  均已令牌化；保留既有管理器蓝色导航状态，作为产品一致性的有意差异。
- 图片与资产：该工作流没有产品图片、品牌插画或非标准图标需求；没有用占位图、
  Emoji、手绘 SVG 或 CSS 图形替代参考资产。
- 文案与内容：从概念图的“迁移”语义收敛为真实的安全会话复制，来源、目标、
  清洗策略、独立线程和共享池状态均使用本机真实数据。

## 聚焦状态与交互证据

- 成功切换 `muyuan` 为来源，账号态随即成为可选目标。
- 成功切回账号态，选择来源会话和 `muyuan` 目标后，摘要同步显示来源、会话、目标和目标模型。
- `创建会话副本` 在条件不完整时禁用，准备完成后启用并显示“准备就绪”。
- 页面控制台无 warning/error。
- 浏览器验收未点击最终迁移按钮，避免在真实共享池中制造额外会话；复制服务已由自动化测试覆盖。
- `design-qa-focused.png` 将参考图的迁移核心区和实现的已选中状态置于同一图中；
  来源、策略、目标、摘要和主按钮均可读，因此无需再拆更小的局部截图。

## 迭代记录

1. 首轮截图发现长工作目录导致来源列表出现横向滚动条。
2. 为迁移列表、卡片和文本行补充最小宽度、溢出裁剪与非收缩约束，保留纵向滚动和完整卡片高度。
3. 发现选择完成后摘要文字仍显示“请选择”，补充准备状态同步；复测按钮启用且摘要为“准备就绪”。

## 剩余差异

- 参考图包含账号认证、厂商监控和批量任务等概念性模块；本实现刻意不伪造这些能力。
- 当前视觉沿用既有 Web 管理器的窗口框架与蓝色导航选中态，同时在迁移内容区采用参考图的暖白和金色语义。

final result: passed
